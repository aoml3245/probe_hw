{
  "name": "proweb_hw",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd client && nodemon server.js",
    "dev": "npm run server  & npm run client"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "nodemon": "^3.1.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
